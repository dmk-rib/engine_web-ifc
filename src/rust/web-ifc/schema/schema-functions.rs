//! Auto-translated property name lookup from schema-functions.cpp.

#![allow(dead_code)]

use super::ifc_schema::IFC_SCHEMA;
use super::schema_names::PROPY_NAMES;

#[inline]
pub fn get_property_name(schema: IFC_SCHEMA, type_code: u32, prop: u32) -> &'static str {
    match schema {
    }
}
